import fs from'node:fs/promises';import{rank}from'./engine.js';const e=process.env,c={lookback:+(e.LOOKBACK_MATCHES||10),minAvg:+(e.MIN_GOALS_AVG||1),minH2H:+(e.MIN_H2H_OVER15||.8),minScored:+(e.MIN_SCORED_LAST5||4),minOver15:+(e.MIN_TEAM_OVER15_LAST5||.8),minBtts:+(e.MIN_RECENT_BTTS||.6)};if(!e.APIFY_TOKEN)throw Error('Mangler APIFY_TOKEN');const date=new Intl.DateTimeFormat('en-CA',{timeZone:e.TIMEZONE||'Europe/Copenhagen',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date()),actor=e.APIFY_ACTOR_ID||'statanow~flashscore-scraper-live',input=e.APIFY_INPUT_JSON?JSON.parse(e.APIFY_INPUT_JSON):{sport:'football',date,withHistory:true,historyLimit:10,maxItems:500},u=`https://api.apify.com/v2/acts/${encodeURIComponent(actor)}/run-sync-get-dataset-items?token=${encodeURIComponent(e.APIFY_TOKEN)}`,r=await fetch(u,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(input)});if(!r.ok)throw Error(`Datakilde ${r.status}: ${await r.text()}`);const matches=await r.json(),out=rank(matches,c);await fs.mkdir('docs/data',{recursive:true});await fs.writeFile('docs/data/results.json',JSON.stringify({date,updatedAt:new Date().toISOString(),totalMatches:matches.length,missing:out.missing,results:out.results.slice(0,50)},null,2));
