import test from'node:test';import assert from'node:assert/strict';import{evaluate,rank}from'../src/engine.js';const g=(h,a,hs,as)=>({home_team:h,away_team:a,home_score:hs,away_score:as}),hist=t=>[g(t,'X',2,1),g('Y',t,1,2),g(t,'Z',1,1),g('Q',t,0,1),g(t,'R',2,1)],config={lookback:10,minAvg:1,minH2H:.8,minScored:4,minOver15:.8,minBtts:.6},match=(home,away)=>({home_team:home,away_team:away,home_history:hist(home),away_history:hist(away),h2h:[g(home,away,2,1),g(away,home,1,1),g(home,away,3,0),g(away,home,2,1),g(home,away,2,2),g(away,home,1,2)]});test('fem kriterier',()=>{let r=evaluate(match('A','B'),config);assert.equal(r.passed,true);assert.equal(r.criteria.length,5)});test('returnerer de ti naermeste kampe',()=>{const results=rank(Array.from({length:12},(_,i)=>match(`H${i}`,`A${i}`)),config).results;assert.equal(results.length,10);assert.equal(results.every(x=>x.status==='ok'),true)});test('returnerer naermeste misses separat',()=>{const output=rank([match('H','A')],{...config,minAvg:10});assert.equal(output.nearMisses.length,1);assert.equal(output.nearMisses[0].passed,false)})
