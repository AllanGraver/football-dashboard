import test from'node:test';import assert from'node:assert/strict';import{evaluate}from'../src/engine.js';const g=(h,a,hs,as)=>({home_team:h,away_team:a,home_score:hs,away_score:as}),hist=t=>[g(t,'X',2,1),g('Y',t,1,2),g(t,'Z',1,1),g('Q',t,0,1),g(t,'R',2,1)];test('fem kriterier',()=>{let r=evaluate({home_team:'A',away_team:'B',home_history:hist('A'),away_history:hist('B'),h2h:[g('A','B',2,1),g('B','A',1,1),g('A','B',3,0),g('B','A',2,1),g('A','B',2,2),g('B','A',1,2)]},{lookback:10,minAvg:1,minH2H:.8,minScored:4,minOver15:.8,minBtts:.6});assert.equal(r.passed,true);assert.equal(r.criteria.length,5)})
